{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "predev": "pnpm run build:libs",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:libs": "pnpm --filter @workspace/api-zod --filter @workspace/api-client-react --filter @workspace/db run build",
    "clean:libs": "node -e \"const fs=require('fs');['lib/api-zod','lib/api-client-react','lib/db'].forEach(p=>{fs.rmSync(p+'/dist',{recursive:true,force:true});['tsconfig.tsbuildinfo','tsconfig.json.tsbuildinfo'].forEach(f=>{try{fs.unlinkSync(p+'/'+f)}catch{}})});['tsconfig.tsbuildinfo'].forEach(f=>{try{fs.unlinkSync(f)}catch{}})\"",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "validate:libs": "pnpm run clean:libs && pnpm run build:libs && pnpm run typecheck:libs"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.4",
    "typescript": "~5.9.3"
  }
}
